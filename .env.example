# CVAT Queue Manager Configuration

# Secret key for JWT tokens (change in production!)
SECRET_KEY=change-this-secret-key-in-production

# Database URL (SQLite by default)
DATABASE_URL=sqlite+aiosqlite:///./queue.db

# Server settings
HOST=0.0.0.0
PORT=8000
